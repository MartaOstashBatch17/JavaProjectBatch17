NESTED LOOPS:
       it's a loop inside another loop, just like nested if statements.
       We can nest any type of loop.
Why do we need them?- to solve complex problems that requires accessing nested data

Arrays:
       is a data1structure tha helps us store multiple items in a single var.
       We can use th indexes to access them.
